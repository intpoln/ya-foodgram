# 🍳 Foodgram

Foodgram — это веб-приложение для публикации и обмена рецептами. Пользователи могут создавать рецепты, добавлять их в избранное, подписываться на других авторов и формировать список покупок для приготовления блюд.

https://polfoodgram.ddns.net/recipes

## 📋 Содержание

- [Описание](#описание)
- [Технологии](#технологии)
- [Функциональность](#функциональность)
- [Установка и запуск](#установка-и-запуск)
- [API документация](#api-документация)
- [Переменные окружения](#переменные-окружения)
- [Команды для работы](#команды-для-работы)
- [Авторы](#авторы)

## 📖 Описание

Foodgram — это полнофункциональное веб-приложение, состоящее из:
- **Backend**: REST API на Django REST Framework
- **Frontend**: Single Page Application на React
- **База данных**: PostgreSQL
- **Веб-сервер**: Nginx

Проект позволяет пользователям:
- Публиковать свои рецепты с фотографиями
- Просматривать рецепты других пользователей
- Добавлять рецепты в избранное
- Подписываться на авторов
- Формировать список покупок
- Скачивать список покупок в текстовом формате

## 🛠 Технологии

### Backend
- **Python 3.12**
- **Django 5.2.7**
- **Django REST Framework 3.16.1**
- **PostgreSQL 14**
- **Gunicorn**
- **Nginx**

### Frontend
- **React 17.0.1**
- **React Router 5.2.0**

### Инфраструктура
- **Docker**
- **Docker Compose**

## ✨ Функциональность

### Для всех пользователей
- Просмотр рецептов на главной странице
- Просмотр детальной информации о рецепте
- Просмотр профилей пользователей
- Регистрация и авторизация
- Фильтрация рецептов по тегам

### Для авторизованных пользователей
- Создание, редактирование и удаление своих рецептов
- Добавление рецептов в избранное
- Подписка на авторов
- Формирование списка покупок
- Скачивание списка покупок в формате TXT
- Изменение пароля
- Загрузка и изменение аватара

### Для администраторов
- Полный доступ к админ-панели Django
- Управление всеми моделями через админку

## 🚀 Установка и запуск

### Предварительные требования

- Docker и Docker Compose установлены на вашем компьютере
- Git для клонирования репозитория

### Клонирование репозитория

```bash
git clone https://github.com/yourusername/foodgram.git
cd foodgram
```

### Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# База данных
DB_ENGINE=django.db.backends.postgresql
POSTGRES_DB=foodgram
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
DB_HOST=db
DB_PORT=5432

# Django
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
```

### Запуск проекта

1. **Соберите и запустите контейнеры:**

```bash
docker compose up -d --build
```

2. **Выполните миграции:**

```bash
docker compose exec backend python manage.py migrate
```

3. **Соберите статические файлы:**

```bash
docker compose exec backend python manage.py collectstatic --no-input
```

4. **Создайте суперпользователя (опционально):**

```bash
docker compose exec backend python manage.py createsuperuser
```

### Доступ к приложению

После успешного запуска приложение будет доступно по адресам:

- **Frontend**: http://localhost:8000
- **Backend API**: http://localhost:8000/api/
- **API Документация (Swagger)**: http://localhost:8000/api/docs/swagger/
- **Админ-панель**: http://localhost:8000/admin/

## 📚 API документация

API документация доступна в двух форматах:

- **Swagger UI**: http://localhost:8000/api/docs/swagger/
### Основные эндпоинты

- `GET /api/recipes/` - Список рецептов
- `POST /api/recipes/` - Создание рецепта (требуется авторизация)
- `GET /api/recipes/{id}/` - Детали рецепта
- `PATCH /api/recipes/{id}/` - Обновление рецепта (требуется авторизация)
- `DELETE /api/recipes/{id}/` - Удаление рецепта (требуется авторизация)
- `GET /api/ingredients/` - Список ингредиентов
- `GET /api/tags/` - Список тегов
- `GET /api/users/` - Список пользователей
- `POST /api/users/` - Регистрация пользователя
- `POST /api/auth/token/login/` - Получение токена авторизации
- `POST /api/auth/token/logout/` - Выход из системы

Полный список эндпоинтов доступен в документации API.

## 🔐 Переменные окружения

Основные переменные окружения, которые необходимо настроить в файле `.env`:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `POSTGRES_DB` | Имя базы данных | `foodgram` |
| `POSTGRES_USER` | Пользователь БД | `postgres` |
| `POSTGRES_PASSWORD` | Пароль БД | `your_password` |
| `DB_HOST` | Хост БД | `db` |
| `DB_PORT` | Порт БД | `5432` |
| `SECRET_KEY` | Секретный ключ Django | `your-secret-key` |
| `DEBUG` | Режим отладки | `False` |
| `ALLOWED_HOSTS` | Разрешенные хосты | `localhost,127.0.0.1,polfoodgram.ddns.net` |
| `CSRF_TRUSTED_ORIGINS` | Разрешенные хосты для CSRF | `https://polfoodgram.ddns.net` |

## 🛠 Команды для работы

### Управление контейнерами

```bash
# Запуск контейнеров
docker compose up -d

# Остановка контейнеров
docker compose down

# Пересборка контейнеров
docker compose up -d --build

# Просмотр логов
docker compose logs -f backend
docker compose logs -f frontend
docker compose logs -f db
```

### Работа с базой данных

```bash
# Выполнение миграций
docker compose exec backend python manage.py migrate

# Создание миграций
docker compose exec backend python manage.py makemigrations

# Откат миграций
docker compose exec backend python manage.py migrate app_name zero
```

### Работа с данными

```bash
# Внесение тестовых данных ингредиентов
docker compose exec backend python manage.py load_ingredients
```

### Работа с Django shell

```bash
# Открыть Django shell
docker compose exec backend python manage.py shell

# Выполнить команду Python
docker compose exec backend python manage.py shell -c "from recipes.models import Recipe; print(Recipe.objects.count())"
```

### Очистка данных

```bash
# Остановить и удалить контейнеры, volumes и сети
docker compose down -v

# Очистить только базу данных (через Django shell)
docker compose exec backend python manage.py shell
>>> from django.contrib.auth import get_user_model
>>> User = get_user_model()
>>> User.objects.all().delete()
```

## 🧪 Тестирование API

В проекте включена Postman коллекция для тестирования API:

1. Импортируйте файл `postman_collection/foodgram.postman_collection.json` в Postman
2. Настройте переменную `baseUrl` на `http://localhost:8000`
3. Запустите коллекцию для проверки всех эндпоинтов

## 📝 Разработка

### Локальная разработка без Docker

1. **Установите зависимости:**

```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Frontend
cd ../frontend
npm install
```

2. **Настройте базу данных:**

Используйте SQLite для разработки (измените `settings.py`) или установите PostgreSQL локально.

3. **Запустите серверы:**

```bash
# Backend
cd backend
python manage.py runserver

# Frontend
cd frontend
npm start
```
## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Авторы

- **intpoln** - [GitHub профиль](https://github.com/intpoln)

## 🙏 Благодарности

- Команда Яндекс.Практикум за предоставление задания
- Сообщество Django и React за отличную документацию

---

**Приятного использования Foodgram! 🍳**
